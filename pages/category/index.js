let categoryTitle = "Tutorial";//get from url

const titleElem = document.getElementById("title");
titleElem.innerText = categoryTitle;

console.log(titleElem);

let articles = [

];

// let displayArticles = filterByCategory(category);

// displayToHtml(displayArticles);